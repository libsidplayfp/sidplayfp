AM_CPPFLAGS = -DHAVE_UNIX -I $(top_builddir)/unix $(SIDPLAY2_CFLAGS)

noinst_LIBRARIES = libwav.a

libwav_a_SOURCES = WavFile.cpp WavFile.h WavFileDefs.h 

# Remove bad default includes
DEFAULT_INCLUDES=
